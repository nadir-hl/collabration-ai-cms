# Database — Neon Postgres connection string
# Get from: https://neon.tech → New Project → Connection String
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Payload secret — generate a strong random string
# Run: openssl rand -hex 32
PAYLOAD_SECRET=your-secret-here

# Next.js public URL (no trailing slash)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Preview secret — authenticates the draft-preview route; generate with: openssl rand -hex 16
PREVIEW_SECRET=your-preview-secret-here
